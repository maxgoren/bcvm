fn fib(let k) {
	if (k < 2) {
		return 1;
	}
	return fib(k-1)+fib(k-2);
}
fn listFibs(let k) {
	if (k < 10) {
		println fib(k);
		listFibs(k+1);
		println k;
		return k;
	}
	println k;
	return k;
}
listFibs(6);