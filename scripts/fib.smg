fn fib(let k) {
	if (k < 2) {
		return 1;
	}
	return fib(k-1)+fib(k-2);
}
fn isEven(let m) {
	return m % 2 == 0;
}
fn cufibs(let m) {
	if (m < 10) {
		if (!isEven(fib(m))) {
			println fib(m);
		}
		return cufibs(m+1);
	}
	return m;
}
cufibs(1);