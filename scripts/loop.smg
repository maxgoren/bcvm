fn fib(let k) {
    if (k < 2) {
        return 1;
    }
    return fib(k-1)+fib(k-2);
} 

fn isEven(let i) {
    return (i % 2 == 0);
}

fn apply(let fun, let xs) {
    return fun(xs); 
}

let i := 1;
while (i < 10) {
    if (apply(isEven, i)) {
        println fib(i);
    }
    i := i + 1;
}