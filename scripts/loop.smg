fn fib(let k) {
    if (k < 2) {
        return 1;
    } else {
        return fib(k-1)+fib(k-2);
    }
} 
let i := 1;
while (i < 10) {
    if (i % 2 == 0) {
        println fib(i);
    }
    i := i + 1;
}